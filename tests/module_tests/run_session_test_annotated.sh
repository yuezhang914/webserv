#!/bin/sh

# ==============================================================================
# 初学者阅读说明
# ==============================================================================
# 这是一份“只增加注释、不改变原命令”的学习版脚本。
# 每条可执行语句前都有“原第 N 行”说明；跨行命令会在整块命令前逐行解释，
# 避免把注释插入反斜杠续行中而破坏 Shell 语法。
#
# 常用符号速查：
#   #          注释开始；本行后面的内容不执行。
#   $VAR       展开变量 VAR 的值。
#   ${VAR}     用花括号明确变量名边界。
#   ${VAR:-x}  VAR 未设置或为空时使用默认值 x，但不会修改 VAR。
#   $0         当前脚本路径；$1、$2 是函数或脚本的位置参数。
#   $?         上一条命令的退出状态；0 表示成功，非 0 表示失败。
#   $(命令)    命令替换：执行命令并把它的标准输出放到当前位置。
#   "..."      双引号：允许 $变量 展开，同时避免空格、* 等被再次拆分或展开。
#   '...'      单引号：内容完全按字面处理，不展开变量。
#   \          行末反斜杠表示下一物理行仍属于同一条命令。
#   ;          在同一物理行结束一条命令，例如 `; then`。
#   &&         左侧成功后才执行右侧。
#   ||         左侧失败后才执行右侧。
#   !          对命令或测试结果取反。
#   |          管道：把左侧标准输出交给右侧标准输入。
#   >文件      覆盖写入标准输出。
#   >>文件     追加写入标准输出。
#   2>文件     重定向文件描述符 2，也就是标准错误。
#   2>&1       让标准错误指向标准输出当前去向。
#   >&2        把本命令标准输出送到标准错误。
#   /dev/null  丢弃写进去的内容。
#   [ 条件 ]   `test` 命令；`[` 与 `]` 两边必须有空格。
#   (...)      子 Shell 或 Bash 数组，具体含义取决于上下文。
#   {...}      当前 Shell 中的命令组；花括号两侧需要空格或换行。
#
# 常用退出码：
#   0          成功/条件为真。
#   非 0       失败/条件为假；本脚本常用 1 表示检查失败、2 表示编译失败。
# ==============================================================================

# 文件：tests/module_tests/run_session_test.sh
# 用途：定位 Webserv 根目录，检查 Session Bonus 文件、注释和代码限制，使用 C++98 严格编译并运行独立测试。
# 测试边界：只编译 srcs/Session，不依赖 Request、Response、ServerManager、Config、socket 或 CGI。
# 清理行为：测试结束后自动删除 .build_session，避免把编译产物留在仓库或交付包中。
# 原第 6 行说明：`set` 修改 Shell 运行选项；`-e` 让未被条件结构接住的失败命令终止脚本，`-u` 让未定义变量报错。
set -eu

# 函数：fail
# 用途：统一输出前置检查或编译准备失败原因，并立即退出。
# 参数：$1 是调用分支传入的错误说明。
# 变量：无局部变量。
# 实现逻辑：向标准错误打印 [FAIL]，再以状态码 1 结束脚本。
# 原第 13 行说明：定义名为 `fail` 的 Shell 函数；空括号表示函数声明本身不写形参，参数通过 `$1`、`$2` 等位置参数读取。
fail()
# 原第 14 行说明：`{` 开始函数体或命令组；在 Shell 中它是语法关键字，不是 C++ 作用域。
{
# 原第 15 行说明：`echo` 输出后面的文本并自动换行；双引号会展开 `$变量`，同时保留文本中的空格；`>&2` 把标准输出重定向到标准错误，便于失败信息与普通输出区分。
    echo "[FAIL] $1" >&2
# 原第 16 行说明：`exit 1` 立即结束整个脚本，并把状态码 1 返回给调用者；0 通常表示成功，非 0 表示失败。
    exit 1
# 原第 17 行说明：`}` 结束函数体或命令组。
}

# 函数：looks_like_project_root
# 用途：判断候选目录是否是正式 Webserv 根目录或可独立测试的 Session 模块根目录。
# 参数：$1 是 find_project_root() 当前检查的目录。
# 变量：candidate 保存位置参数，便于多次使用。
# 实现逻辑：Makefile 与 srcs 同时存在时接受；独立包具有 srcs/Session 和 include(s) 时也接受。
# 原第 24 行说明：定义名为 `looks_like_project_root` 的 Shell 函数；空括号表示函数声明本身不写形参，参数通过 `$1`、`$2` 等位置参数读取。
looks_like_project_root()
# 原第 25 行说明：`{` 开始函数体或命令组；在 Shell 中它是语法关键字，不是 C++ 作用域。
{
# 原第 26 行说明：给变量 `candidate` 赋值；Shell 赋值号两侧不能有空格。右侧若带双引号，可保持路径完整。
    candidate=$1
# 原第 27 行说明：开始 `if` 条件判断。`-f` 判断普通文件是否存在；`-d` 判断目录是否存在；`&&` 表示左侧成功才检查右侧
    if [ -f "$candidate/Makefile" ] && [ -d "$candidate/srcs" ]; then
# 原第 28 行说明：`return 0` 从当前函数返回退出状态 0；Shell 中 0 表示成功，非 0 表示失败。
        return 0
# 原第 29 行说明：`fi` 是 `if` 的反向拼写，用来结束当前条件分支。
    fi
# 原第 30 行说明：开始 `if` 条件判断。`-d` 判断目录是否存在
# 原第 31 行说明：`&&` 表示只有上一条命令成功（退出码 0）时，才继续执行右侧命令。
    if [ -d "$candidate/srcs/Session" ] \
        && { [ -d "$candidate/includes" ] || [ -d "$candidate/include" ]; }
# 原第 32 行说明：`then` 开始执行条件为真的分支；它也可以写在条件行的 `; then` 后。
    then
# 原第 33 行说明：`return 0` 从当前函数返回退出状态 0；Shell 中 0 表示成功，非 0 表示失败。
        return 0
# 原第 34 行说明：`fi` 是 `if` 的反向拼写，用来结束当前条件分支。
    fi
# 原第 35 行说明：`return 1` 从当前函数返回退出状态 1；Shell 中 0 表示成功，非 0 表示失败。
    return 1
# 原第 36 行说明：`}` 结束函数体或命令组。
}

# 函数：find_project_root
# 用途：从测试脚本目录逐层向上查找项目根目录。
# 参数：无；起点来自全局 SCRIPT_DIR。
# 变量：candidate 保存当前候选目录。
# 实现逻辑：每层调用 looks_like_project_root()；找到时输出绝对路径，到 / 仍未找到则失败。
# 原第 43 行说明：定义名为 `find_project_root` 的 Shell 函数；空括号表示函数声明本身不写形参，参数通过 `$1`、`$2` 等位置参数读取。
find_project_root()
# 原第 44 行说明：`{` 开始函数体或命令组；在 Shell 中它是语法关键字，不是 C++ 作用域。
{
# 原第 45 行说明：给变量 `candidate` 赋值；Shell 赋值号两侧不能有空格。右侧若带双引号，可保持路径完整。
    candidate="$SCRIPT_DIR"
# 原第 46 行说明：开始 `while` 循环；方括号 `[...]` 实际是 `test` 命令。`!=` 比较字符串不相等，只要候选目录还不是 `/` 就继续向上查找。
    while [ "$candidate" != "/" ]
# 原第 47 行说明：`do` 开始 `for` 或 `while` 循环体。
    do
# 原第 48 行说明：本行继续完成当前 Shell 操作；`$变量`/`${...}` 展开变量或参数，双引号允许变量展开并阻止空白拆词。
        if looks_like_project_root "$candidate"; then
# 原第 49 行说明：`printf` 按格式输出。`%s` 放入字符串参数，`\n` 表示换行；与 `echo` 相比，格式和转义行为更可预测。
            printf '%s\n' "$candidate"
# 原第 50 行说明：`return 0` 从当前函数返回退出状态 0；Shell 中 0 表示成功，非 0 表示失败。
            return 0
# 原第 51 行说明：`fi` 是 `if` 的反向拼写，用来结束当前条件分支。
        fi
# 原第 52 行说明：给变量 `candidate` 赋值；Shell 赋值号两侧不能有空格。右侧若带双引号，可保持路径完整。
        candidate=$(dirname "$candidate")
# 原第 53 行说明：`done` 结束 `for` 或 `while` 循环体。
    done
# 原第 54 行说明：`return 1` 从当前函数返回退出状态 1；Shell 中 0 表示成功，非 0 表示失败。
    return 1
# 原第 55 行说明：`}` 结束函数体或命令组。
}

# 函数：find_header_directory
# 用途：确定项目公共头文件目录使用 includes/ 还是 include/。
# 参数：无；在 PROJECT_ROOT 已确定并 cd 后调用。
# 变量：无局部变量。
# 实现逻辑：优先选择已经包含 SessionStore.hpp 的目录，否则选择现有目录；都不存在时失败。
# 原第 62 行说明：定义名为 `find_header_directory` 的 Shell 函数；空括号表示函数声明本身不写形参，参数通过 `$1`、`$2` 等位置参数读取。
find_header_directory()
# 原第 63 行说明：`{` 开始函数体或命令组；在 Shell 中它是语法关键字，不是 C++ 作用域。
{
# 原第 64 行说明：开始 `if` 条件判断。`-f` 判断普通文件是否存在
    if [ -f "includes/SessionStore.hpp" ]; then
# 原第 65 行说明：`printf` 按格式输出。`%s` 放入字符串参数，`\n` 表示换行；与 `echo` 相比，格式和转义行为更可预测。
        printf '%s\n' "includes"
# 原第 66 行说明：`return 0` 从当前函数返回退出状态 0；Shell 中 0 表示成功，非 0 表示失败。
        return 0
# 原第 67 行说明：`fi` 是 `if` 的反向拼写，用来结束当前条件分支。
    fi
# 原第 68 行说明：开始 `if` 条件判断。`-f` 判断普通文件是否存在
    if [ -f "include/SessionStore.hpp" ]; then
# 原第 69 行说明：`printf` 按格式输出。`%s` 放入字符串参数，`\n` 表示换行；与 `echo` 相比，格式和转义行为更可预测。
        printf '%s\n' "include"
# 原第 70 行说明：`return 0` 从当前函数返回退出状态 0；Shell 中 0 表示成功，非 0 表示失败。
        return 0
# 原第 71 行说明：`fi` 是 `if` 的反向拼写，用来结束当前条件分支。
    fi
# 原第 72 行说明：开始 `if` 条件判断。`-d` 判断目录是否存在
    if [ -d "includes" ]; then
# 原第 73 行说明：`printf` 按格式输出。`%s` 放入字符串参数，`\n` 表示换行；与 `echo` 相比，格式和转义行为更可预测。
        printf '%s\n' "includes"
# 原第 74 行说明：`return 0` 从当前函数返回退出状态 0；Shell 中 0 表示成功，非 0 表示失败。
        return 0
# 原第 75 行说明：`fi` 是 `if` 的反向拼写，用来结束当前条件分支。
    fi
# 原第 76 行说明：开始 `if` 条件判断。`-d` 判断目录是否存在
    if [ -d "include" ]; then
# 原第 77 行说明：`printf` 按格式输出。`%s` 放入字符串参数，`\n` 表示换行；与 `echo` 相比，格式和转义行为更可预测。
        printf '%s\n' "include"
# 原第 78 行说明：`return 0` 从当前函数返回退出状态 0；Shell 中 0 表示成功，非 0 表示失败。
        return 0
# 原第 79 行说明：`fi` 是 `if` 的反向拼写，用来结束当前条件分支。
    fi
# 原第 80 行说明：`return 1` 从当前函数返回退出状态 1；Shell 中 0 表示成功，非 0 表示失败。
    return 1
# 原第 81 行说明：`}` 结束函数体或命令组。
}

# 函数：cleanup_build
# 用途：删除本脚本生成的测试可执行文件和临时构建目录。
# 参数：无；使用全局 BUILD_DIR，变量可能在脚本前置失败时尚未设置。
# 变量：无局部变量。
# 实现逻辑：BUILD_DIR 非空时执行 rm -rf，确保正常退出和信号退出都不留下产物。
# 原第 88 行说明：定义名为 `cleanup_build` 的 Shell 函数；空括号表示函数声明本身不写形参，参数通过 `$1`、`$2` 等位置参数读取。
cleanup_build()
# 原第 89 行说明：`{` 开始函数体或命令组；在 Shell 中它是语法关键字，不是 C++ 作用域。
{
# 原第 90 行说明：开始 `if` 条件判断。`!=` 表示字符串不相等
    if [ "${BUILD_DIR:-}" != "" ]; then
# 原第 91 行说明：`rm` 删除文件或目录；`-r` 递归删除目录内容，`-f` 强制删除且文件不存在时不报错。变量放在双引号中可防止路径中的空格被拆开。
        rm -rf "$BUILD_DIR"
# 原第 92 行说明：`fi` 是 `if` 的反向拼写，用来结束当前条件分支。
    fi
# 原第 93 行说明：`}` 结束函数体或命令组。
}

# 原第 95 行说明：计算并保存脚本自身所在目录的绝对路径。`$0` 是脚本路径，`dirname` 取父目录，`$(...)` 捕获命令输出；`CDPATH=` 临时清空 cd 搜索路径，`cd --` 中 `--` 结束选项解析，`&&` 仅在 cd 成功后执行 `pwd`。
SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
# 原第 96 行说明：计算项目根目录。`$(...)` 是命令替换；`../..` 从测试脚本目录向上两级，`&&` 保证前一个 `cd` 成功后才执行 `pwd`；双引号保护路径空格。
# 原第 97 行说明：`||` 表示只有上一条命令失败（退出码非 0）时，才执行右侧的失败处理。
PROJECT_ROOT=$(find_project_root) \
    || fail "找不到 Webserv 项目根目录：脚本应位于 <项目>/tests/module_tests/，根目录需包含 Makefile 和 srcs/"
# 原第 98 行说明：`cd` 切换当前工作目录；路径使用双引号，避免目录名中的空格被拆词。
cd "$PROJECT_ROOT"
# 原第 99 行说明：`printf` 按格式输出。`%s` 放入字符串参数，`\n` 表示换行；与 `echo` 相比，格式和转义行为更可预测。
printf '%s\n' "[PASS] 已定位项目根目录：$PROJECT_ROOT"

# 原第 101 行说明：调用 `find_header_directory` 并用 `$(...)` 捕获输出；行末 `\` 把命令续到下一行，下一行 `|| fail ...` 表示查找失败才调用失败函数。
# 原第 102 行说明：`||` 表示只有上一条命令失败（退出码非 0）时，才执行右侧的失败处理。
HEADER_DIR=$(find_header_directory) \
    || fail "项目根目录中既没有 includes/，也没有 include/"
# 原第 103 行说明：`printf` 按格式输出。`%s` 放入字符串参数，`\n` 表示换行；与 `echo` 相比，格式和转义行为更可预测。
printf '%s\n' "[PASS] 使用头文件目录：$HEADER_DIR"

# 原第 105 行说明：选择 C++ 编译器。`${CXX:-c++}` 表示：环境变量 `CXX` 已设置且非空时使用它，否则使用默认值 `c++`；`:-` 是带默认值的参数展开。
CXX=${CXX:-c++}
# 原第 106 行说明：保存编译选项字符串：`-Wall` 开启常见警告，`-Wextra` 开启额外警告，`-Werror` 把警告当错误，`-std=c++98` 强制 C++98；`-pedantic-errors`（若出现）把非标准扩展视为错误；`-I目录` 添加头文件搜索目录。
BASE_CXXFLAGS="-Wall -Wextra -Werror -std=c++98 -pedantic-errors ${EXTRA_CXXFLAGS:-}"
# 原第 107 行说明：保存测试构建目录路径；测试可执行文件放在这里，退出时统一删除。
BUILD_DIR="tests/module_tests/.build_session"
# 原第 108 行说明：`trap` 为退出或信号注册清理函数。`EXIT` 是正常/异常退出，`HUP`、`INT`、`TERM` 分别是挂起、中断和终止信号；收到时先执行清理。
trap cleanup_build EXIT HUP INT TERM
# 原第 109 行说明：`rm` 删除文件或目录；`-r` 递归删除目录内容，`-f` 强制删除且文件不存在时不报错。变量放在双引号中可防止路径中的空格被拆开。
rm -rf "$BUILD_DIR"
# 原第 110 行说明：`mkdir` 创建目录；`-p` 会同时创建缺失的父目录，并在目录已经存在时不报错。
mkdir -p "$BUILD_DIR"

# 文件检查：逐个报告缺失文件，避免把复制不完整误报成根目录错误。
# 原第 113 行说明：开始 `for` 循环；`in` 后的单词依次赋给循环变量。变量列表未加引号时会按空白拆词，这里各项本身都是无空格路径。
# 原第 114 行说明：这是上一条跨行命令或字符串的续行；末尾 `\` 取消换行，下一物理行仍属于同一条语句。本行通常提供一个文件、目录或参数。
# 原第 115 行说明：这是上一条跨行命令或字符串的续行；末尾 `\` 取消换行，下一物理行仍属于同一条语句。本行通常提供一个文件、目录或参数。
# 原第 116 行说明：这是上一条跨行命令或字符串的续行；末尾 `\` 取消换行，下一物理行仍属于同一条语句。本行通常提供一个文件、目录或参数。
# 原第 117 行说明：这是上一条跨行命令或字符串的续行；末尾 `\` 取消换行，下一物理行仍属于同一条语句。本行通常提供一个文件、目录或参数。
# 原第 118 行说明：这是上一条跨行命令或字符串的续行；末尾 `\` 取消换行，下一物理行仍属于同一条语句。本行通常提供一个文件、目录或参数。
# 原第 119 行说明：这是上一条跨行命令或字符串的续行；末尾 `\` 取消换行，下一物理行仍属于同一条语句。本行通常提供一个文件、目录或参数。
# 原第 120 行说明：这是前面源码/头文件列表或编译命令中的一个路径参数；Shell 会把它作为独立文件传给检查命令或编译器。
for file in \
    "$HEADER_DIR/SessionStore.hpp" \
    "$HEADER_DIR/SessionCookie.hpp" \
    "$HEADER_DIR/SessionDemo.hpp" \
    srcs/Session/SessionStore.cpp \
    srcs/Session/SessionCookie.cpp \
    srcs/Session/SessionDemo.cpp \
    tests/module_tests/session_module_test.cpp
# 原第 121 行说明：`do` 开始 `for` 或 `while` 循环体。
 do
# 原第 122 行说明：方括号是 `test` 命令的另一种写法；结尾 `]` 是必需参数。；`-f` 检查普通文件，`||` 左边失败时执行右边
    [ -f "$file" ] || fail "缺少 Session 模块文件：$file"
# 原第 123 行说明：`done` 结束 `for` 或 `while` 循环体。
 done

# 接口检查：确认模块覆盖 Cookie、Session 管理、ID 轮换、退出删除和简单示例。
# 原第 126 行说明：`grep` 搜索文本。`-q` 静默模式，只看退出码；`$` 匹配行尾
# 原第 127 行说明：`||` 表示只有上一条命令失败（退出码非 0）时，才执行右侧的失败处理。
grep -q 'class SessionStore' "$HEADER_DIR/SessionStore.hpp" \
    || fail "SessionStore.hpp 未定义 SessionStore"
# 原第 128 行说明：`grep` 搜索文本。`-q` 静默模式，只看退出码；`$` 匹配行尾
# 原第 129 行说明：`||` 表示只有上一条命令失败（退出码非 0）时，才执行右侧的失败处理。
grep -q 'class SessionCookie' "$HEADER_DIR/SessionCookie.hpp" \
    || fail "SessionCookie.hpp 未定义 SessionCookie"
# 原第 130 行说明：`grep` 搜索文本。`-q` 静默模式，只看退出码；`$` 匹配行尾
# 原第 131 行说明：`||` 表示只有上一条命令失败（退出码非 0）时，才执行右侧的失败处理。
grep -q 'class SessionDemo' "$HEADER_DIR/SessionDemo.hpp" \
    || fail "SessionDemo.hpp 未定义 SessionDemo"
# 原第 132 行说明：`grep` 搜索文本。`-q` 静默模式，只看退出码；`$` 匹配行尾
# 原第 133 行说明：`||` 表示只有上一条命令失败（退出码非 0）时，才执行右侧的失败处理。
grep -q 'regenerateSession' "$HEADER_DIR/SessionStore.hpp" \
    || fail "SessionStore 缺少 ID 轮换接口"
# 原第 134 行说明：`grep` 搜索文本。`-q` 静默模式，只看退出码；`$` 匹配行尾
# 原第 135 行说明：`||` 表示只有上一条命令失败（退出码非 0）时，才执行右侧的失败处理。
grep -q 'buildExpiredCookie' "$HEADER_DIR/SessionCookie.hpp" \
    || fail "Cookie 模块缺少删除 Cookie 接口"
# 原第 136 行说明：`grep` 搜索文本。`-q` 静默模式，只看退出码；`$` 匹配行尾
# 原第 137 行说明：`||` 表示只有上一条命令失败（退出码非 0）时，才执行右侧的失败处理。
grep -q 'buildCounterExample' "$HEADER_DIR/SessionDemo.hpp" \
    || fail "SessionDemo 缺少访问计数示例"
# 原第 138 行说明：`grep` 搜索文本。`-q` 静默模式，只看退出码；`$` 匹配行尾
# 原第 139 行说明：`||` 表示只有上一条命令失败（退出码非 0）时，才执行右侧的失败处理。
grep -q 'buildLoginExample' "$HEADER_DIR/SessionDemo.hpp" \
    || fail "SessionDemo 缺少登录示例"
# 原第 140 行说明：`grep` 搜索文本。`-q` 静默模式，只看退出码；`$` 匹配行尾
# 原第 141 行说明：`||` 表示只有上一条命令失败（退出码非 0）时，才执行右侧的失败处理。
grep -q 'buildLogoutExample' "$HEADER_DIR/SessionDemo.hpp" \
    || fail "SessionDemo 缺少退出示例"

# 结构检查：正式 C++ 文件和测试 C++ 文件不能定义命名空间，也不能残留 .inc 拆分文件。
# 原第 144 行说明：开始 `if` 条件判断。Shell 根据命令退出码判断真假：0 为真，非 0 为假。
# 原第 145 行说明：这是上一条跨行命令或字符串的续行；末尾 `\` 取消换行，下一物理行仍属于同一条语句。本行通常提供一个文件、目录或参数。
# 原第 146 行说明：这是上一条跨行命令或字符串的续行；末尾 `\` 取消换行，下一物理行仍属于同一条语句。本行通常提供一个文件、目录或参数。
# 原第 147 行说明：这是上一条跨行命令或字符串的续行；末尾 `\` 取消换行，下一物理行仍属于同一条语句。本行通常提供一个文件、目录或参数。
# 原第 148 行说明：这是上一条跨行命令或字符串的续行；末尾 `\` 取消换行，下一物理行仍属于同一条语句。本行通常提供一个文件、目录或参数。
# 原第 149 行说明：这是前面源码/头文件列表或编译命令中的一个路径参数；Shell 会把它作为独立文件传给检查命令或编译器。
if grep -R -n -E '(^|[^[:alnum:]_])namespace([^[:alnum:]_]|$)' \
    "$HEADER_DIR/SessionStore.hpp" \
    "$HEADER_DIR/SessionCookie.hpp" \
    "$HEADER_DIR/SessionDemo.hpp" \
    srcs/Session \
    tests/module_tests/session_module_test.cpp >/dev/null 2>&1; then
# 原第 150 行说明：本行继续完成当前 Shell 操作；双引号允许变量展开并阻止空白拆词。
    fail "Session 模块仍使用 namespace"
# 原第 151 行说明：`fi` 是 `if` 的反向拼写，用来结束当前条件分支。
fi
# 原第 152 行说明：开始 `if` 条件判断。Shell 根据命令退出码判断真假：0 为真，非 0 为假。
if find srcs/Session -type f -name '*.inc' | grep . >/dev/null 2>&1; then
# 原第 153 行说明：本行继续完成当前 Shell 操作；双引号允许变量展开并阻止空白拆词。
    fail "Session 模块不应包含 .inc 实现片段"
# 原第 154 行说明：`fi` 是 `if` 的反向拼写，用来结束当前条件分支。
fi

# 注释检查：确认每个类、结构体和测试入口都有顶部中文说明，并检查包含区写明库用途。
# 原第 157 行说明：开始 `for` 循环；`in` 后的单词依次赋给循环变量。变量列表未加引号时会按空白拆词，这里各项本身都是无空格路径。
# 原第 158 行说明：这是上一条跨行命令或字符串的续行；末尾 `\` 取消换行，下一物理行仍属于同一条语句。本行通常提供一个文件、目录或参数。
# 原第 159 行说明：这是上一条跨行命令或字符串的续行；末尾 `\` 取消换行，下一物理行仍属于同一条语句。本行通常提供一个文件、目录或参数。
# 原第 160 行说明：这是上一条跨行命令或字符串的续行；末尾 `\` 取消换行，下一物理行仍属于同一条语句。本行通常提供一个文件、目录或参数。
# 原第 161 行说明：本行继续完成当前 Shell 操作；单引号按字面保存内容，不展开变量和反斜杠。
for marker in \
    '类：SessionStore' \
    '类：SessionCookie' \
    '类：SessionDemo' \
    '结构体：SessionDemoResult'
# 原第 162 行说明：`do` 开始 `for` 或 `while` 循环体。
 do
# 原第 163 行说明：`grep` 搜索文本。`-R` 递归搜索目录；`-q` 静默模式，只看退出码；`$` 匹配行尾
# 原第 164 行说明：`||` 表示只有上一条命令失败（退出码非 0）时，才执行右侧的失败处理。
    grep -R -q "$marker" "$HEADER_DIR" \
        || fail "缺少类或结构体顶部注释：$marker"
# 原第 165 行说明：`done` 结束 `for` 或 `while` 循环体。
 done
# 原第 166 行说明：开始 `for` 循环；`in` 后的单词依次赋给循环变量。变量列表未加引号时会按空白拆词，这里各项本身都是无空格路径。
# 原第 167 行说明：这是上一条跨行命令或字符串的续行；末尾 `\` 取消换行，下一物理行仍属于同一条语句。本行通常提供一个文件、目录或参数。
# 原第 168 行说明：这是上一条跨行命令或字符串的续行；末尾 `\` 取消换行，下一物理行仍属于同一条语句。本行通常提供一个文件、目录或参数。
# 原第 169 行说明：这是上一条跨行命令或字符串的续行；末尾 `\` 取消换行，下一物理行仍属于同一条语句。本行通常提供一个文件、目录或参数。
# 原第 170 行说明：这是上一条跨行命令或字符串的续行；末尾 `\` 取消换行，下一物理行仍属于同一条语句。本行通常提供一个文件、目录或参数。
# 原第 171 行说明：这是上一条跨行命令或字符串的续行；末尾 `\` 取消换行，下一物理行仍属于同一条语句。本行通常提供一个文件、目录或参数。
# 原第 172 行说明：这是上一条跨行命令或字符串的续行；末尾 `\` 取消换行，下一物理行仍属于同一条语句。本行通常提供一个文件、目录或参数。
# 原第 173 行说明：这是前面源码/头文件列表或编译命令中的一个路径参数；Shell 会把它作为独立文件传给检查命令或编译器。
for file in \
    "$HEADER_DIR/SessionStore.hpp" \
    "$HEADER_DIR/SessionCookie.hpp" \
    "$HEADER_DIR/SessionDemo.hpp" \
    srcs/Session/SessionStore.cpp \
    srcs/Session/SessionCookie.cpp \
    srcs/Session/SessionDemo.cpp \
    tests/module_tests/session_module_test.cpp
# 原第 174 行说明：`do` 开始 `for` 或 `while` 循环体。
 do
# 原第 175 行说明：`grep` 搜索文本。`-q` 静默模式，只看退出码；`$` 匹配行尾
# 原第 176 行说明：`||` 表示只有上一条命令失败（退出码非 0）时，才执行右侧的失败处理。
    grep -q '包含：' "$file" \
        || fail "包含库区域缺少用途注释：$file"
# 原第 177 行说明：`done` 结束 `for` 或 `while` 循环体。
 done
# 原第 178 行说明：`grep` 搜索文本。`-q` 静默模式，只看退出码
# 原第 179 行说明：`||` 表示只有上一条命令失败（退出码非 0）时，才执行右侧的失败处理。
grep -q '函数：main' tests/module_tests/session_module_test.cpp \
    || fail "Session 测试 main 缺少函数顶部注释"

# 交付检查：源码目录中不能残留常见编译产物；测试脚本自己不属于该检查范围。
# 原第 182 行说明：开始 `if` 条件判断。Shell 根据命令退出码判断真假：0 为真，非 0 为假。
# 原第 183 行说明：这是上一条跨行命令或字符串的续行；末尾 `\` 取消换行，下一物理行仍属于同一条语句。本行通常提供一个文件、目录或参数。
# 原第 184 行说明：这是上一条跨行命令或字符串的续行；末尾 `\` 取消换行，下一物理行仍属于同一条语句。本行通常提供一个文件、目录或参数。
# 原第 185 行说明：`|` 把左侧命令的标准输出作为右侧命令的标准输入，组成管道。
if find srcs/Session "$HEADER_DIR" tests/module_tests \
    -type f \( -name '*.o' -o -name '*.a' -o -name '*.so' \
    -o -name '*.dylib' -o -name 'session_module_test' \) \
    | grep . >/dev/null 2>&1; then
# 原第 186 行说明：本行继续完成当前 Shell 操作；双引号允许变量展开并阻止空白拆词。
    fail "Session 目录中残留编译产物"
# 原第 187 行说明：`fi` 是 `if` 的反向拼写，用来结束当前条件分支。
fi

# 原第 189 行说明：`printf` 按格式输出。`%s` 放入字符串参数，`\n` 表示换行；与 `echo` 相比，格式和转义行为更可预测。
printf '%s\n' "[PASS] Cookie 解析与 Set-Cookie 接口存在"
# 原第 190 行说明：`printf` 按格式输出。`%s` 放入字符串参数，`\n` 表示换行；与 `echo` 相比，格式和转义行为更可预测。
printf '%s\n' "[PASS] Session 生命周期与 ID 轮换接口存在"
# 原第 191 行说明：`printf` 按格式输出。`%s` 放入字符串参数，`\n` 表示换行；与 `echo` 相比，格式和转义行为更可预测。
printf '%s\n' "[PASS] counter/login/logout 简单示例存在"
# 原第 192 行说明：`printf` 按格式输出。`%s` 放入字符串参数，`\n` 表示换行；与 `echo` 相比，格式和转义行为更可预测。
printf '%s\n' "[PASS] C++ 文件未使用 namespace 或 .inc"
# 原第 193 行说明：`printf` 按格式输出。`%s` 放入字符串参数，`\n` 表示换行；与 `echo` 相比，格式和转义行为更可预测。
printf '%s\n' "[PASS] 类、函数和包含库注释检查通过"
# 原第 194 行说明：`printf` 按格式输出。`%s` 放入字符串参数，`\n` 表示换行；与 `echo` 相比，格式和转义行为更可预测。
printf '%s\n' "[PASS] 交付目录没有编译产物"

# 原第 196 行说明：开始给变量 `SESSION_SOURCES` 赋一个跨多行的字符串列表。行末反斜杠 `\` 取消本行换行，使下一物理行继续属于同一个字符串。
# 原第 197 行说明：这是上一条跨行命令或字符串的续行；末尾 `\` 取消换行，下一物理行仍属于同一条语句。本行通常提供一个文件、目录或参数。
# 原第 198 行说明：这是前面源码/头文件列表或编译命令中的一个路径参数；Shell 会把它作为独立文件传给检查命令或编译器。
SESSION_SOURCES="srcs/Session/SessionStore.cpp \
srcs/Session/SessionCookie.cpp \
srcs/Session/SessionDemo.cpp"

# 原第 200 行说明：调用 C++ 编译器。未加引号的 flags/source 列表需要按空格拆成多个参数；`-I` 添加头文件搜索路径，`-o` 指定输出可执行文件。编译器路径本身加引号可保护空格。
# 原第 201 行说明：这是上一条跨行命令或字符串的续行；末尾 `\` 取消换行，下一物理行仍属于同一条语句。本行通常提供一个文件、目录或参数。
# 原第 202 行说明：这是上一条跨行命令或字符串的续行；末尾 `\` 取消换行，下一物理行仍属于同一条语句。本行通常提供一个文件、目录或参数。
# 原第 203 行说明：本行继续完成当前 Shell 操作；`$变量`/`${...}` 展开变量或参数，双引号允许变量展开并阻止空白拆词。
"$CXX" $BASE_CXXFLAGS -I"$HEADER_DIR" \
    tests/module_tests/session_module_test.cpp \
    $SESSION_SOURCES \
    -o "$BUILD_DIR/session_module_test"

# 原第 205 行说明：`printf` 按格式输出。`%s` 放入字符串参数，`\n` 表示换行；与 `echo` 相比，格式和转义行为更可预测。
printf '%s\n' "[PASS] Session Bonus 模块编译成功"
# 原第 206 行说明：运行刚编译出的测试可执行文件；双引号保护构建目录路径中的空格。
"$BUILD_DIR/session_module_test"
